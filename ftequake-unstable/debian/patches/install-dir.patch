--- a/engine/Makefile
+++ b/engine/Makefile
@@ -1936,14 +1936,20 @@
 endif
 
 
-prefix ?= /usr/local
+prefix ?= /usr
 exec_prefix ?= $(prefix)
-bindir ?= $(exec_prefix)/bin
+bindir ?= $(exec_prefix)/games
 sbindir ?= $(exec_prefix)/sbin
 INSTALL ?= install
 INSTALL_PROGRAM ?= $(INSTALL)
 INSTALL_DATA ?= ${INSTALL} -m 644
-install: sv-rel gl-rel mingl-rel qcc-rel
-	$(INSTALL_PROGRAM) $(RELEASE_DIR)/fteqw $(DESTDIR)$(bindir)/fteqw
-	$(INSTALL_PROGRAM) $(RELEASE_DIR)/fteqwsv $(DESTDIR)$(bindir)/fteqwsv
+install: sv-rel gl-rel mingl-rel qcc-rel vk-rel sv-dbg gl-dbg vk-dbg
+	# standard targets
+	$(INSTALL_PROGRAM) $(RELEASE_DIR)/fteqw.gl $(DESTDIR)$(bindir)/fteqw-gl
+	$(INSTALL_PROGRAM) $(RELEASE_DIR)/fteqw.sv $(DESTDIR)$(bindir)/fteqw-sv
 	$(INSTALL_PROGRAM) $(RELEASE_DIR)/fteqcc $(DESTDIR)$(bindir)/fteqcc
+	$(INSTALL_PROGRAM) $(RELEASE_DIR)/fteqw_vk $(DESTDIR)$(bindir)/fteqw-vk
+	# debug targets
+	$(INSTALL_PROGRAM) $(DEBUG_DIR)/fteqw.sv $(DESTDIR)$(bindir)/fteqw-sv-dbg
+	$(INSTALL_PROGRAM) $(DEBUG_DIR)/fteqw.gl $(DESTDIR)$(bindir)/fteqw-gl-dbg $(DESTDIR)$(bindir)/fteqw-gl-dbg
+	$(INSTALL_PROGRAM) $(DEBUG_DIR)/fteqw_vk $(DESTDIR)$(bindir)/fteqw-vk-dbg
