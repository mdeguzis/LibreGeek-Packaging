#!/usr/bin/make -f

%:
	dh $@ --parallel -Bbuild

override_dh_auto_configure:
	rm -rf build && mkdir build
	cd build && cmake \
            -DCMAKE_INSTALL_PREFIX=/usr \
            ..
	make -C build -j4

override_dh_auto_test:
