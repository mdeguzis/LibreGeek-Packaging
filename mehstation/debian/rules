#!/usr/bin/make -f

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1

%:
	dh $@

override_dh_auto_configure:
	cmake -DCMAKE_INSTALL_PREFIX:PATH=/usr .

override_dh_auto_install:
	$(MAKE) DESTDIR=$(CURDIR)/debian/mehstation PREFIX=/usr install
