#!/usr/bin/make -f

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1

# Set versions for install files
so_version="5.6.0"

%:
	dh $@
	
override_dh_auto_configure:
	qmake -r CONFIG+=release

override_dh_auto_build:
	dh_auto_build -- INSTALL_ROOT=$(CURDIR)/debian/tmp/usr
