#!/usr/bin/make -f

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1

%:
	dh $@
	
override_dh_auto_build:
	qmake -r CONFIG+=release
	make

override_dh_builddeb:
	# intentionally break here to take a look at built files
	cp BLAH BLAH
