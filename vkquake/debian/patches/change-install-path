--- a/Quake/Makefile
+++ b/Quake/Makefile
@@ -315,4 +315,4 @@
 	rm -f $(shell find . \( -name '*~' -o -name '#*#' -o -name '*.o' -o -name '*.res' -o -name $(DEFAULT_TARGET) \) -print)
 
 install:	quakespasm
-	cp quakespasm /usr/local/games/quake
+	cp quakespasm /usr/games/quake
